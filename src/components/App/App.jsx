import Autocomplete from '../Autocomplete';

import './App.css';

const App = () => (
  <main className="App">
    <header className="App-header">
      Movies Autocomplete
    </header>
    <section>
      <Autocomplete />
    </section>
  </main>
);

export default App;
